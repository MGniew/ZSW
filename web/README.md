# UI

### Requirements
* npm
* bower

### Install dependencies
* `bower install`
* `npm install connect`
* `npm install serve-static`


### Run
* `node server.js`
 by default UI runs on `localhost:8080`
